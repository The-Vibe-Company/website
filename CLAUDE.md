Use agent-browser to visit the website. Never use playwright or Claude Chrome. 
Use the skill agent-browser to know how to use the CLI. 
If agent-browser is not installed, install it.

To run agent-browser, use the following command:
```
agent-browser
```


When you open a PR, always include screenshots of the website.